tech:
- node 14.17.0
- typescript
- apolo server
- graphql
- postgresDB
- javascript esm
- Sequelize.js for ORM

guide:
- install node (try node 14.17.0)
- install typescript
- npm install
- "npm start" to start using proyek
- Create the model in DB
- check name of db you use


 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "start": "npm run compile && node ./src/index.js"
  },